import { DefaultTheme } from 'styled-components'

export const DEFAULT_THEME_TITLE = 'DEFAULT_THEME_TITLE'

const THEME: DefaultTheme = {
  title: DEFAULT_THEME_TITLE,
  textColor: '#fff',
  backgroundColor: '#121212',
}

export default THEME
