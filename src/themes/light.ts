import { DefaultTheme } from "styled-components";

export const LIGHT_THEME_TITLE = "LIGHT";

const THEME: DefaultTheme = {
  title: LIGHT_THEME_TITLE,
  textColor: "#333",
  backgroundColor: "#fff",
};

export default THEME;
